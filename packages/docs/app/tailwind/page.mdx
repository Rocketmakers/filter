# filter-builder · Tailwind (shadcn)

A shadcn-style filter builder using **Tailwind CSS** + **shadcn/ui**
primitives.

> Docs are still being written. See the [Mantine variant](../mantine) for the
> full feature tour — the API is identical across variants; only the styling
> stack changes.

## Next steps

- **[Install](./install)** — drop the component into a Tailwind/shadcn app with one command.
- **[Integrations](./integrations)** — wire it up to your backend.
