# Install · Tailwind (shadcn)

Coming soon. The CLI entry-point will be:

```sh
pnpm dlx @rocketmakers/filter tailwind
```

In the meantime, see the [Mantine install guide](../../mantine/install) — the
file layout is equivalent; only the styling primitives differ (Tailwind
utility classes + shadcn components instead of Mantine + SCSS).
