# Install · Style-X

Coming soon. The CLI entry-point will be:

```sh
pnpm dlx @rocketmakers/filter stylex
```

In the meantime, see the [Mantine install guide](../../mantine/install) — the
file layout and provider wiring are equivalent; only the styling layer
differs.
