import { IntegrationsPage } from "../../components/integrations-page";

# Integrations

Pick how you persist filter state and which backend you want to query
against. Each tile shows the exact code you'd paste into your app —
production-shaped, with the variant toggles (e.g. server-side vs.
in-memory, REST vs. GraphQL) you'd realistically swap between.

<IntegrationsPage />
